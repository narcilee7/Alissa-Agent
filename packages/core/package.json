{
  "name": "@alissa/core",
  "version": "0.0.1",
  "description": "Core Agent and LLM management functionality",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "test:unit": "vitest run --reporter=verbose",
    "test:coverage": "vitest run --coverage",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@alissa/shared": "workspace:*",
    "@alissa/providers": "workspace:*",
    "@alissa/monitoring": "workspace:*",
    "zod": "^3.22.4",
    "rxjs": "^7.8.1",
    "reflect-metadata": "^0.1.13",
    "inversify": "^6.0.1",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "peerDependencies": {
    "@alissa/shared": "workspace:*",
    "@alissa/providers": "workspace:*"
  }
}
