{
  "name": "@alissa/kafka",
  "version": "0.0.1",
  "description": "Kafka producers and consumers",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "test:unit": "vitest run --reporter=verbose",
    "test:coverage": "vitest run --coverage",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@alissa/shared": "workspace:*",
    "@alissa/monitoring": "workspace:*",
    "kafkajs": "^2.2.4",
    "avsc": "^5.7.7",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
